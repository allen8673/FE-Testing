import './section-editor.scss'
import MarkdownIt from 'markdown-it';
const SectionEditor = ():JSX.Element =>{
    const mdParser = new MarkdownIt(/* Markdown-it options */);
    
    return <div id='section-editor'>
        
    </div>
}

export default SectionEditor